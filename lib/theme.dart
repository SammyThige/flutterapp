/* import 'package:flutter/material.dart';
import 'package:lost_n_found/Pages/chats.dart';

ThemeData lightThemeData(BuildContext context) {
  return ThemeData.light().copyWith(
    primaryColor: Color(0xFF00BF6D),
    scaffoldBackgroundColor: Colors.blue,
    iconTheme: IconThemeData(
      color: Color(0xFF1D1D35),
    ),
    /* colorScheme: ColorScheme.light(
          primary: Color(0xFF00BF6D), secondary: Color(0xFFFE99901)) */
  );
}

ThemeData darkThemeData(BuildContext context) {
  return ThemeData.dark().copyWith(
      primaryColor: Color.fromARGB(255, 7, 7, 7),
      scaffoldBackgroundColor: Colors.black,
      iconTheme: IconThemeData(
        color: Color(0xFF1D1D35),
      )
      /* colorScheme: ColorScheme.dark().copyWith(
          primary: Color.fromRGBO(143, 148, 251, 1), secondary: Color(0xFF))*/
      );
}
 */